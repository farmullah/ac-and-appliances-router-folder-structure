import React from 'react';
import Layout from '../components/Layout';

import "../styles/geoTagging.css";

const GeoTagging = () => {
    return <Layout>Geo tagging</Layout>;
};

export default GeoTagging;
